<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-8">
  <h1 class="text-2xl font-bold text-center mb-4"><%= @welcome_message %></h1>

  <% if !session[:user] %>
    <div class="text-center space-x-4">
      <a href="/login" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Login</a>
      <a href="/register" class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600">Register</a>
    </div>
  <% else %>
    <div class="text-center">
      <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Logout</a>
    </div>
  <% end %>
</body>
</html>
